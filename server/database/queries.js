// module.exports = {
// 	insertNewUser: "insert into events_manager.users(`username`, `password`, `email`, `groups_id`) values (?, ?, ?, ?)",

// 	checkIfUserExists: (name) => { return `select count(*) from users where username = '${name}'` }

// };